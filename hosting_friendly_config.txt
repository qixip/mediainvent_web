import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "export",
  images: { unoptimized: true },
  trailingSlash: true,
  assetPrefix: "",
  basePath: "",
  experimental: {
    // Use a different directory structure that's more hosting-friendly
    outputFileTracingRoot: "",
  },
};